apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.0
  name: ipampools.controller.kubeslice.io
spec:
  group: controller.kubeslice.io
  names:
    kind: IPAMPool
    listKind: IPAMPoolList
    plural: ipampools
    singular: ipampool
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IPAMPool is the Schema for the ipampools API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: IPAMPoolSpec defines the desired state of IPAMPool
            properties:
              allocationStrategy:
                default: Efficient
                enum:
                - Sequential
                - Random
                - Efficient
                type: string
              autoReclaim:
                default: true
                type: boolean
              maxClusters:
                default: 16
                maximum: 32
                minimum: 2
                type: integer
              poolType:
                default: Dynamic
                enum:
                - Dynamic
                - Static
                type: string
              reclaimDelay:
                default: 3600
                minimum: 300
                type: integer
              reservedSubnets:
                items:
                  type: string
                type: array
              sliceSubnet:
                type: string
              subnetSize:
                type: string
            required:
            - sliceSubnet
            - subnetSize
            type: object
          status:
            description: IPAMPoolStatus defines the observed state of IPAMPool
            properties:
              allocatedCount:
                type: integer
              allocatedSubnets:
                additionalProperties:
                  description: ClusterSubnet represents a subnet allocated to a specific cluster
                  properties:
                    allocatedAt:
                      description: 'CreationTimestamp is a timestamp representing the server time when this object was created. It is not guaranteed to be set in happens-before order across separate operations. Clients may not set this value. It is represented in RFC3339 form and is in UTC.'
                      format: date-time
                      type: string
                    clusterName:
                      type: string
                    labels:
                      additionalProperties:
                        type: string
                      type: object
                    lastUsedAt:
                      format: date-time
                      type: string
                    status:
                      enum:
                      - Active
                      - Inactive
                      - PendingReclamation
                      type: string
                    subnetCIDR:
                      type: string
                  required:
                  - allocatedAt
                  - clusterName
                  - status
                  - subnetCIDR
                  type: object
                type: object
              availableCount:
                type: integer
              availableSubnets:
                items:
                  type: string
                type: array
              conditions:
                items:
                  description: "Condition contains details for one aspect of the current state of this API Resource. --- This struct is intended for direct use with the Kubernetes API (https://github.com/kubernetes/api/blob/master/objects.go#L1354). For higher level logic, use APIResourceConditionType instead."
                  properties:
                    lastTransitionTime:
                      description: "lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable."
                      format: date-time
                      type: string
                    message:
                      description: "message is a human readable message indicating details about the transition. This may be an empty string."
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: "observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance."
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: "reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string.  This field may not be empty."
                      maxLength: 1024
                      type: string
                    status:
                      description: "status of the condition, one of True, False, Unknown."
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: "type of condition in CamelCase or in foo.example.com/CamelCase. --- Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be useful (see .node.status.conditions), the ability to deconflict is important. The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)"
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9]*[A-Za-z0-9])|[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - observedGeneration
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastAllocationTime:
                format: date-time
                type: string
              lastReclamationTime:
                format: date-time
                type: string
              reservedSubnets:
                items:
                  type: string
                type: array
              totalSubnets:
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
